# ==============================================================================
# LOGTO SSO CONFIGURATION
# ==============================================================================
# Logto application endpoint (from your Logto console)
NEXT_PUBLIC_LOGTO_ENDPOINT=https://your-tenant.logto.app

# Logto application ID (public, can be exposed to client)
NEXT_PUBLIC_LOGTO_APP_ID=your-logto-app-id

# Logto application secret (KEEP SECRET, server-side only)
LOGTO_APP_SECRET=your-logto-app-secret

# Cookie secret for session management (KEEP SECRET, generate strong random string)
# Generate with: openssl rand -base64 32
LOGTO_COOKIE_SECRET=your-cookie-secret-32-characters-minimum

# ==============================================================================
# APPLICATION URLS
# ==============================================================================
# Base URL of your application (used by Logto for redirects)
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# OAuth callback URL (must match Logto app configuration)
LOGTO_REDIRECT_URI=https://your-domain.com/api/logto/callback

# Post sign-out redirect URL
LOGTO_POST_SIGNOUT_URI=https://your-domain.com

# ==============================================================================
# NEXTAUTH CONFIGURATION
# ==============================================================================
# Secret key for NextAuth.js sessions (generate a strong random string)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-32-characters-minimum

# Canonical URL of your site (used by NextAuth.js)
NEXTAUTH_URL=https://your-domain.com

# ==============================================================================
# DEVELOPMENT SETTINGS
# ==============================================================================
# Set to development for local development
NODE_ENV=production